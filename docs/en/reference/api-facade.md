# API Facade

`API` is the thin public facade that groups Adapter-side factories and integration helpers.

Use it when you want named queue creation, cache helpers, serializer helpers, payload-handler registration, and public package composition in one place.
