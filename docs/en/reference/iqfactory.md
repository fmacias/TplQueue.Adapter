# IQFactory

Use `IQFactory` to create runtime queues such as `IParallelQ`, `IFifoQ`, and `ICacheQ`.

Use `IQFactoryAdapter` when you want named queue creation backed by adapter-side configuration dictionaries.
