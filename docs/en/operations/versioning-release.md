# Versioning and Release

The active public preview line is `0.1.0-preview.1`.

For coordinated release work, keep package version, git tag, and GitHub release title aligned across the publishable `Fmacias.TplQueue.*` packages.
