# Strong-Name Signing

Normal source builds are unsigned.

Official signed release packages are produced only when the packaging pipeline receives an external private `.snk` path and the matching full public key.
